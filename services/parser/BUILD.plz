subinclude("//build/defs:service_version")
subinclude("///pleasings//docker")

docker_image(
    name="parser",
    srcs = glob(["*"], ["bin", "obj", "dist"]),
    dockerfile = "Dockerfile",
    visibility = ["//services/parser/..."]
)

service_version(
    name = "version",
    service = "parser",
    srcs = ["git-version.yml", "//:git"],
)