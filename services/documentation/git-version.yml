mode: ContinuousDeployment
# uncomment this to get to first version
# Note: this is because of a bug because of which +semver: major upgrades only minor versions
# https://github.com/GitTools/GitVersion/issues/1539

ignore:
  sha: []
merge-message-formats: {}
tag-prefix: services/documentation/v
# next-version: 1.0
