subinclude("//build/defs:service_version")
subinclude("///pleasings//docker")

docker_image(
  name = "documentation",
  srcs = glob(["*"], ["node_modules", "dist"]),
  dockerfile = "Dockerfile",
  visibility = ["//services/documentation/..."]
)

service_version(
  name = "version",
  service = "documentation",
  srcs = glob(["git-version.yml"]) + [ "//:git"],
)
